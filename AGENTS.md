# AGENTS.md

このリポジトリでCodexが作業する時の必須ルール。

## 1. 作業開始前に必ず読むこと

実装、設計変更、UI変更、README更新、テスト追加の前に、必ず以下を読む。

- `docs/spec.md`
- `docs/product_context.md`

この2つに反する実装判断をしてはいけない。迷った場合は、まずドキュメントを更新するか、ユーザーに確認する。

## 2. プロダクトの本質

定番メシは「食事記録アプリ」ではない。

定番メシは、お気に入りの店で自分のいつもの注文をすぐ思い出すための「定番注文を即参照するアプリ」である。

最重要の利用シーン:

- 店に入る直前
- レジ前
- 券売機前
- 注文直前
- 食後に新しい組み合わせを短時間で残す時

実装判断では、記録量より即参照性を優先する。

## 3. MVPで優先すること

必ず優先すること:

- 即時性
- 検索
- 高密度表示
- ローカル保存
- ログインなし
- 店名だけで登録できること
- 注文内容を複数行で素早く入力できること
- 「定番」「次試す」「試した」を整理できること
- 店詳細で「定番 -> 次試す -> 試した」の順に表示すること

## 3.5 画面実装の必須条件

ホーム画面:

- 検索バーを入口の中心にする
- 検索未入力時は最近見た店と新規登録導線を中心にする
- 注文セットをタイムラインのように大量表示しない
- 検索結果は高密度リストで表示する
- 登録済み店の全件表示はホームに混ぜず、一覧タブに分離する
- 一覧タブでは店名順と追加順で店を探せるようにする
- 店削除は一覧タブから確認付きで行い、通常導線として目立たせすぎない

店詳細画面:

- 注文直前に読む画面として設計する
- 必ず「定番 -> 次試す -> 試した」の順で表示する
- 注文内容をテキスト主体でコンパクトに表示する
- 写真や大きなカードで縦に長くしすぎない
- 「＋ この店で追加」を必ず置く
- 注文セットの状態変更は、店詳細の左スワイプ操作で素早く行えるようにする

登録、編集画面:

- 店名と注文内容だけで保存に近づける
- 注文内容は複数行自由入力にする
- 最後に空行が自動追加される入力体験を重視する
- メイン、サイド、トッピングなどの分類を強制しない
- 注文セット編集画面では店名を変更させない
- 店名変更は店詳細の専用導線で行い、既存の注文メモが別店舗へ移動したように見える挙動を避ける
- 新規登録時は似た店名のサジェストを表示し、既存店への紐づけを優先できるようにする
- 似た店名がある時に新規店を作る場合は、重複防止の確認を挟む

## 4. MVPで実装しないこと

MVPでは以下を実装しない。

- ログイン
- アカウント作成
- SNS
- ユーザー間共有
- 地図
- 店舗検索
- 店舗レビュー
- クラウド同期
- AI提案
- 決済、実注文
- 店舗向け機能
- 価格専用欄
- カロリー管理
- 店舗住所、営業時間登録

## 5. 避けるUI

以下の方向に寄せない。

- 大きなカードUI
- 写真SNS風UI
- タイムライン風UI
- 店舗レビューアプリ風UI
- 店舗検索アプリ風UI
- 余白過多の一覧
- 写真が主役の詳細画面
- 入力項目が多すぎるフォーム

写真機能は必ず提供するが、写真入力は任意とする。店の代表写真と注文写真を別データとして扱い、表示はコンパクトなサムネイル中心にする。注文内容のテキストが主役である。

## 6. 実装時の技術方針

MVPの想定技術:

- SwiftUI
- SwiftData
- PhotosPicker
- iOS 17以降
- ローカル保存
- 通信なし
- 認証なし

ただし、このリポジトリでまだXcodeプロジェクトが作成されていない段階では、勝手にSwiftUI実装やXcodeプロジェクト作成を始めない。ユーザーから実装開始の明示指示が出てから行う。

## 7. データ設計の原則

基本エンティティは以下。

- `Shop`
- `OrderSet`

`Shop` は店名を表す。`OrderSet` はその店での注文セットを表す。

注文セットの状態は以下の3つだけをMVPで扱う。

- `favorite`: 定番
- `want_to_try`: 次試す
- `tried`: 試した

注文内容は複数行自由入力で、内部的には文字列配列として扱う。メイン、サイド、トッピングのような意味づけはしない。

## 8. 判断に迷った時

判断に迷ったら、以下を確認する。

- 注文直前にすぐ読めるか
- 店名検索からすぐ辿り着けるか
- 登録が10秒以内に近い感覚で終わるか
- 写真、評価、レビューが主役になっていないか
- 食事記録アプリ化していないか

この基準を満たさない変更は、MVPでは避ける。

## 9. 成長機能のルール

MVP公開後のアップデートでは、次を優先する。

- 店のピン留め
- 店ごとの第一定番
- ホームから第一定番への短い導線
- 日本語検索の正規化と複数語検索
- 注文セットの複製
- バックアップ、復元
- デザインとアクセシビリティの改善

店の「ピン留め」と注文セットの状態である「定番」は別概念である。モデル名や画面文言で混同してはいけない。

## 10. 無料公開のルール

- すべての機能を無料で提供する
- 店、注文セット、写真、店ピン留めの件数を制限しない
- ピン留め店の手動並び替えを全員が利用できるようにする
- 広告を表示しない
- アプリ内課金、Pro案内、購入復元のUIを実装しない
- 広告SDKや購入SDKを導入しない
- 収益化を再検討する場合は、実装前に仕様、プライバシー、App Store申告を更新し、ユーザーの明示的な判断を得る

## 11. 実装順のルール

1. 即参照性、検索、登録速度の不具合をなくす
2. 初回利用、継続利用、アクセシビリティ、デザインを改善する
3. バックアップ、ウィジェット、Spotlightなど継続価値を検討する
4. 利用実績と品質が十分になった後にのみ、収益化の是非を別途検討する

収益化機能を先に入れて、中心体験の品質改善を後回しにしてはいけない。現在は収益化を実装しない。

## 12. 確定デザインのルール

- デザインテイストはWarm Utilityとする
- 温かい白、白い表面、朱色、アンバー、セージ、墨色を役割別に使う
- 店一覧は52pt程度の店写真を使うコンパクト型とする
- 店詳細は高さ90pt程度の店写真と56pt程度の注文写真を使うバランス型とする
- 注文登録では注文写真を中心にし、店写真は店詳細の管理導線から設定する
- 検索中は通常ホーム要素を畳み、写真付き検索結果を優先する
- 大きなカード、過度な角丸、余白過多へ戻さない

## 13. 画像利用のルール

- ユーザーが撮影または写真ライブラリから選択する導線を提供する
- チェーン公式候補は利用許諾を確認できた画像だけ使用する
- ネット検索、食べログ、グルメサイトなどの画像を無断収集、保存、転載しない
- 外部画像APIは保存制限、帰属表示、リンク要件を守る
- 公式候補には「公式」、AI生成画像には「AIイメージ」と表示する
- AI画像を実際の店舗や商品の公式写真として扱わない
- 候補がある時は公式候補を先に表示し、候補がない時は撮影と写真ライブラリを先に表示する
- ユーザーが入力した店名、注文内容、メモを本人の操作なく画像生成サービスへ送信しない
